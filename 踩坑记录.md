# 踩坑记录

> 项目开发过程中遇到的各种问题、调试经验、参数调优历程。
> 供后续实验和算法改进时参考。

---

## 目录

1. [参数调优历程](#一参数调优历程)
2. [分支定界节点爆炸问题](#二分支定界节点爆炸问题)
3. [公式版本的选择](#三公式版本的选择)
4. [数据生成方式的差异](#四数据生成方式的差异)
5. [算法对比结论](#五算法对比结论)
6. [设计决策与方案选择](#六设计决策与方案选择)
7. [项目整理记录](#七项目整理记录)

---

## 一、参数调优历程

### 1.1 论文原始参数（无法复现的原因）

论文第四章第4.5.1节（第72页）的参数设置与最初代码不一致：

| 参数 | 论文值 | 原代码 | 问题 |
|:---|:---:|:---:|:---|
| 种群大小 | 30 | 50 | 原代码写死 |
| 交叉概率 | 0.7 | 未显式设置 | 实际可能为 0 |
| 变异概率 | 0.2 | 0.5 | 太高导致随机搜索 |
| delta（减排成本系数） | 5000 | 随机生成 | 每个实验不一样 |
| 编码位数 | 10 | 5 | 精度只有 0.03 |
| 库存成本 h | 10（论文表5-1） | 1 | 早期错误值 |

### 1.2 参数调整历程

#### 第一轮：delta 扫描（固定其他参数为论文标准值）

在 3×10 规模下，保持其他参数不变，跑不同 delta：

| δ | 总利润 | 碳排放 | 利润提升 | w范围 | 评价 |
|:---|---|---|---|---|---|
| 1000 | 750,001 | 6,268 | +36.2% | 0.79~0.99 | 过度投资减排 |
| **3000** | **608,577** | **99,415** | **+10.6%** | **0.35~0.51** | **✅ 最优平衡点** |
| 4000 | 591,772 | 110,966 | +7.5% | 0.26~0.34 | 偏保守 |
| 5000 | 581,357 | 102,126 | +5.6% | 0.21~0.27 | 论文原值，提升不足 |
| 7000 | 569,648 | 132,319 | +3.5% | 0.13~0.21 | 接近不减排 |
| 10000 | 561,503 | 187,512 | +2.0% | 0.09~0.12 | 几乎不减排 |

**结论：δ=3000 使 w 在 0.35~0.51 的合理范围，利润提升 10.6%，碳排放降低 34.3%，是平衡最优选择。**

#### 第二轮：碳价灵敏度分析（固定 δ=3000, 3×10）

| 碳价 p̂ | 总利润 | 碳排放 | 利润提升 | w范围 |
|:---|---|---|---|---|
| 1.0 | 619,804 | 128,659 | +2.8% | 0.10~0.25 |
| **2.0** | **607,851** | **89,089** | **+10.6%** | **0.35~0.54** |

> p̂≥3 的结果待补充。

### 1.3 最终选定的参数

**δ=3000, p̂=2** 作为基准配置。理由：
- w 在 0.35~0.51 的合理范围（不极端）
- 利润提升 10.6%，减排 34.3%（双效益显著）
- 在 √D 投资公式下比 δ=5000 更平衡

### 1.4 实验结果汇总

| 规模 | 总利润 | 碳排放 | 时间(s) | 基准利润 | 利润提升 | 减排比例 |
|:---|---|---|---|---|---|---|
| 3×5 | 420,719 | 75,189 | 1.09 | 397,949 | **+5.7%** | - |
| 3×10 | 608,577 | 99,415 | 3.95 | 550,499 | **+10.6%** | **-34.3%** |
| 5×10 | 804,597 | 106,084 | 5.91 | 745,018 | **+8.0%** | - |

### 1.5 实验命令速查

```bash
cd ~/projects/cplex_code/4_重构代码

# 单次运行（默认3×10, δ=3000）
make run

# 编译批量实验工具
make experiments

# 批量实验
../bin/run src/0_experiments/table4_1.yaml     # 不同规模
../bin/run src/0_experiments/delta_scan.yaml   # delta灵敏度扫描
../bin/run src/0_experiments/carbon_price_sweep.yaml  # 碳价灵敏度
../bin/run src/0_experiments/full_results.yaml # 完整规模测试
```

---

## 二、分支定界节点爆炸问题

### 2.1 问题描述

原代码（GA_BP.cpp）分支定界节点数超过 **3900 个**，无法在合理时间内完成求解。理论上论文规模（3 DC × 10 Rts）应该只有少量分支节点。

### 2.2 根因分析

1. **剪枝失效**：`best_lowerbound` 在分支过程中没有正确更新，导致剪枝条件永远不满足
2. **根节点 LP 解可能被误判为非整数**：论文显示根节点通常就是整数解（Gap≈0），但原代码因精度问题或对偶变量处理有误触发不必要的分支
3. **搜索策略**：原代码使用 BFS（广度优先），不如 DFS 能更快找到可行整数解

### 2.3 修复方案

- 分支定界改用 **DFS（深度优先）** 策略
- 选 **最分数变量** 分支（most fractional branching）
- 正确更新上下界
- 配置参数：`max_branch_nodes: 10000`, `use_dfs: true`

### 2.4 修复效果

修复后节点数从 3900+ 降为 **0**（根节点 LP 解即为整数解），求解时间大幅降低。

---

## 三、公式版本的选择

### 3.1 投资成本公式

论文标准版（当前使用）：`½ × δ × w_j² × √D_j`

| 版本 | 公式 | 效果 |
|:---|:---|:---|
| ✅ 论文标准版 | `½δw²√D` | w 随 D_j 自适应，大 DC 减排更贵 |
| ❌ 简化版 | `½βw²` | 与 D_j 无关，大 DC 和小 DC 减排成本一样 |

### 3.2 运输成本公式（分段函数）

**供应商 → DC：c(a_j)**
| 距离 a_j | 单位运输成本 |
|:---|---:|
| ≤ 100 | 5 |
| ≤ 300 | 10 |
| ≤ 600 | 15 |
| ≤ 1000 | 20 |
| > 1000 | 25 |

**DC → 零售商：c(d_ij)**
| 距离 d_ij | 单位运输成本 |
|:---|---:|
| ≤ 100 | 10 |
| ≤ 300 | 20 |
| ≤ 600 | 30 |
| ≤ 1000 | 40 |
| > 1000 | 50 |

### 3.3 碳交易公式

净碳交易成本 = p̂ × (E - C)，其中：
- E = 总碳排放
- C = 初始碳配额（100,000）
- E > C → 需购买 e⁺ = E - C
- E < C → 可出售 e⁻ = C - E

---

## 四、数据生成方式的差异

### 4.1 论文数据生成规则

| 项目 | 规则 |
|:---|---|
| 坐标 | [0,1000]×[0,1000] 平面均匀随机，供应商固定在 (500,500) |
| 需求 μ_i | [2000, 3500] 均匀随机 |
| 需求方差 σ²_i | [10, 15] 均匀随机 |
| DC固定成本 f_j | [7000, 10000] 均匀随机 |
| 订货固定成本 F_j | 100 |
| 运输固定成本 g_j | 100 |
| 补货提前期 L_j | 1 |
| 碳配额价格 p̂ | 2 |
| 初始碳配额 C | 100000 |
| 单位库存碳排放 ĥ | 2 |
| 运输碳排放系数 k | 0.01 |
| 设施碳排放 f̂_j | f_j / 10 |
| 零售商保留价 v_i | [60, 80] 均匀随机 |
| 服务水平 α | 97.5%，z_α = 1.96 |

### 4.2 legacy_mode 兼容

`DataGenerator` 支持两种模式：
- **standard**（默认）：完全按论文规则生成
- **legacy**：模拟原代码 GA_BP.cpp 的数据生成方式（可能存在偏差）

通过 `config.yaml` 的 `legacy_mode: true/false` 切换。

---

## 五、算法对比结论

### 5.1 GA+BP vs w=0 基准（证明 GA 搜索 w 的价值）

| 规模 | GA+BP 总利润 | w=0 基准利润 | **利润提升** | w=0 碳排放 | GA+BP 碳排放 | **减排比例** |
|:---|---|---|---|---|---|---|
| 3×5 | 420,616 | 397,949 | **+5.7%** | 78,740 | 57,627 | **-26.8%** |
| 3×10 | 607,851 | 550,499 | **+10.6%** | 111,267 | 89,089 | **-19.9%** |
| 5×10 | 800,297 | 745,018 | **+8.0%** | 159,695 | 107,248 | **-32.8%** |

**结论**：GA 搜索 w 带来利润提升 5.7%~10.6%，碳排放降低 20%~33%。

### 5.2 算法扩展性

| 规模 | 总利润 | 碳排放 | 求解时间 | #DC开 | #R服务 |
|:---|---|---|---|---|---|
| 3×5 | 420,616 | 57,627 | **0.05s** | 3 | 4 |
| 3×10 | 607,851 | 89,089 | **0.13s** | 3 | 9 |
| 5×10 | 800,297 | 107,248 | **0.17s** | 4 | 10 |

> 小规模（3×5~5×10）均在 0.2 秒内完成。

### 5.3 方法说明

**GA+BP 算法原理**：
- **外层**：遗传算法（GA）搜索减排率 w_j（10 位二进制编码，0~1 连续变量）
- **内层**：分支定价（BP）精确求解固定 w 下的选址-分配-定价问题
  - BP 内部使用 CPLEX 求解限制主问题（RMP）的 LP 松弛
  - 定价子问题用参数线性规划（凸包枚举）生成新列
  - 分支定界修复整数性

**为什么不需要对比纯 CPLEX**：BP 的 RMP 求解使用 CPLEX 作为 LP 求解器，因此对于固定 w 的子问题，BP 给出的解就是 CPLEX 能给出的该整数规划的最优解。GA 的作用是搜索 w，BP 对每个 w 精确求解。

---

## 六、设计决策与方案选择

### 6.1 6层架构体系

```
第0层  实验配置层      experiments/    实验方案YAML
第1层  启动器层        launcher/       main入口，编排
第2层  参数配置层      config/ + .yaml  问题参数、公式开关
第3层  公式计算层      formula/        01~09 纯函数
第4层  求解算法层      algorithm/      GA→BP→CG→PS+BB
第5层  结果输出层      postprocessor/  后处理+报告

基础层  base/          日志/随机数/CPLEX封装
核心数据 core/          Types/Instance/Solution
数据层   data/          生成/加载
```

### 6.2 关键设计决策

| 决策项 | 选择 | 原因 |
|:---|---:|:---|
| 配置方式 | YAML（手写解析器） | 不依赖 yaml-cpp，轻量 |
| 分支定界策略 | DFS + 最分数变量分支 | 修复 BFS 剪枝失效问题 |
| 列生成加列策略 | 每轮加所有检验数>0的列 | 减少 CG 迭代次数 |
| 遗传算法策略 | 精英保留 + w=0 初始化 | 保证收敛，保证不差于基准 |
| 投资成本扣除 | 在 BP 层统一扣除 | 投资成本不依赖服务集合 S |
| 公式层独立 | 拆分为 01~09 独立文件 | 可逐项验证与论文一致性 |

### 6.3 调整记录

| 调整项 | 状态 | 说明 |
|:---|:---:|:---|
| 创建 0_experiments/ 目录 | ✅ | 含多个 YAML 实验方案和 run.cpp |
| JSON 实验方案 → YAML | ✅ | 支持全字段覆盖 |
| 创建 1_launcher/ | ✅ | main.cpp + ExperimentRunner.h |
| 创建 0_base/ 合并基础工具 | ✅ | Types/Instance/DataGenerator/Random/Logger/Metrics/CplexWrapper |
| Config.h 支持实验列表 | ✅ | ExperimentEntry + YAML 解析增强 |
| 删除旧的 utils/ 和 solvers/ | ✅ | |
| 删除旧入口文件 | ✅ | |
| 更新 Makefile | ✅ | 根目录 Makefile + src/Build.mk |
| h=1 改为 h=10 | ✅ | 对齐论文表5-1 |
| 投资成本加 √D_j | ✅ | 对齐论文公式 |
| 投资成本用 delta 代替 beta | ✅ | 通过 formula/05 |

---

## 七、项目整理记录

### 7.1 目录清理（2026-07-19）

清理前根目录有 20 个条目，存在大量冗余：

| 清理对象 | 处理 | 原因 |
|:---|---:|:---|
| `3_原版代码/` | 已删除 | 原单文件代码，已保留在 git 历史中 |
| `实验结果/`（两个同名目录） | 已删除 | 旧的实验输出，87MB |
| `实验结果_delta3000/` | 已删除 | 旧的实验输出，106MB |
| `.claude_tmp/` | 已删除 | 临时调试文件 |
| `run_benchmark.sh` | 已删除 | 旧脚本，已被 Makefile 替代 |
| `run_single.sh` | 已删除 | 旧脚本，已被 Makefile 替代 |
| `参数总表.md` | 内容合并至此文件 | 参数文档已归档 |
| `实验计划.md` | 内容合并至此文件 | 实验规划已归档 |
| `实验结果追踪.md` | 内容合并至此文件 | 实验结果已归档 |
| `算法对比实验方案.md` | 内容合并至此文件 | 算法对比方案已归档 |
| `算法对比结果.md` | 内容合并至此文件 | 算法对比结果已归档 |
| `项目整理.md` | 内容合并至此文件 | 项目整理记录已归档 |

### 7.2 清理后项目结构

```
cplex_code/
├── 4_重构代码/              ← 唯一的工作区
│   ├── config.yaml          ← 配置文件
│   ├── Makefile             ← 编译入口
│   ├── results/             ← 运行结果输出
│   └── src/                 ← 源代码
│       ├── 0_base/          ← 基础层
│       ├── 0_experiments/   ← 实验方案（YAML）
│       ├── 1_launcher/      ← 启动层
│       ├── 2_config/        ← 配置层
│       ├── 3_formula/       ← 公式层
│       ├── 4_algorithm/     ← 算法层
│       ├── 5_postprocessor/ ← 后处理层
│       ├── 9_test/          ← 测试
│       └── Build.mk
├── bin/                     ← 编译好的可执行文件
├── .ai/                     ← 项目知识库文档
├── 0硕士原版论文/           ← 论文资料（保留）
├── 1投稿论文latex/          ← 投稿论文源码（保留）
├── 2论文返修意见/           ← 审稿意见（保留）
├── 踩坑记录.md              ← 本文件（经验总结）
├── PROJECT.md              ← 核心项目文档
└── README.md               ← 项目说明
```

### 7.3 性能诊断小技巧（2026-07-21）

在 4×30（δ=3000）大规模性能调优中沉淀的经验：

**（1）并行不等于更快——当心破坏缓存与丢失计时**

定价子问题并行化的朴素实现（每轮每 DC `new` 一个局部 `PricingSolver`）比串行**慢 4.7 倍**（807s vs 169s）。原因：

- `PricingSolver::cachedBestCols` 缓存上一次最优列，相邻迭代对偶变化小时缓存命中率极高，走**快路径 O(零售商数)**；未命中才进凸包枚举**慢路径 O(候选价格数 × n²)**，贵几十倍。
- 局部 solver 每轮新建 → 缓存恒空、每次全走慢路径；且 `pricingTime` 随对象析构丢失，报告 PS=0，时间全落进 `cg_other` 余项，造成"热点在 CG 循环"的假象。

**正确做法**：每 DC 一个**持久** `PricingSolver` 成员数组 `pricingSolvers_[numDC]`，线程 j 用 `pricingSolvers_[j]`——按 DC 下标天然分区、无需加锁，缓存跨迭代保留、计时可回收。详见 [`.ai/08_加速方案.md`](.ai/08_加速方案.md)。

**（2）减法余项会骗人**

`cg_other = cgTime − cplexBuild − cplexSolve − pricing` 是纯减法余项，任何"没被单独计时"的时间都会堆进它。定位时应给循环内各环节加**成员累加器**（在任意 return 点记账），而不是局部变量（提前 return / 析构会漏记）。实测 CG 循环四项（对偶提取/整数检查/result 填充/加列去重）合计仅 **1.75s**，证伪了"CG 循环是热点"的直觉——**真瓶颈是定价 153s（90%）**。

**（3）先证实再优化**：不要凭 O(列数) 之类的直觉推断热点，务必先用计时器实测。本次直觉推断被实测彻底推翻。

### 7.4 目录清理记录（2026-07-21）

`5_重构代码v2/` 根目录在调试过程中累积了大量临时产物（grep 输出、进度快照、构建/运行日志、各种 check 文件），以及项目根的临时补丁脚本。以下为待清理清单（**须手动执行，SSH 免密未配置**）：

```bash
cd "5_重构代码v2"
rm -f grep_*.txt prog*.txt final*.txt \
      build_5x20.log build_5x20_v2.log build.log build_result.txt \
      build_tail.txt build_tail2.txt build_tail3.txt \
      run_5x20.log run_5x20_v2.log run_check.txt run_pid.txt \
      run_tail.txt run_tail_b.txt \
      br.txt lc.txt ls_root.txt sed_done.txt timecheck.txt \
      cfg_check.txt chk_bin.txt ps_check.txt res_ls.txt
cd ..
rm -f tmp_patch_bp.py tmp_patch_ga.py run_started.txt
```

保留项：`Makefile`、`src/`、`results_single/`（实验结果）、`验证报告.md`。
