# 架构设计方案：面向对象的模块化重构（第二版）

> 本文档是第二版重构的完整架构设计。
> 在现有 `4_重构代码/` 的基础上，升级为：
> **面向对象 + 可插拔策略 + 三阶段生命周期 + 三分监控/日志/结果** 的架构。
>
> 最后更新：2026-07-19

---

## 一、总体架构

### 1.1 十大模块

```
┌──────────────────────────────────────────────────────────────────┐
│                       1. 实验模块 (Experiment)                    │
│   定义"跑哪些实验"：实验方案 YAML、实验列表、参数覆盖               │
└───────────────────────┬──────────────────────────────────────────┘
                        │ 调用
┌───────────────────────▼──────────────────────────────────────────┐
│                       2. 启动器模块 (Launcher)                    │
│   程序入口：解析命令行、选择配置文件、组装编排者、触发运行           │
└───────────────────────┬──────────────────────────────────────────┘
                        │ 创建并启动
┌───────────────────────▼──────────────────────────────────────────┐
│                       3. 编排模块 (Orchestrator) ⭐               │
│   管理完整生命周期：initialize() → run() → finalize()             │
│   是整个项目的"大脑"，控制流程走向                                   │
└───┬───────┬───────┬───────┬───────┬───────┬───────┬─────────────┘
    │       │       │       │       │       │       │
    ▼       ▼       ▼       ▼       ▼       ▼       ▼
 ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐
 │ 4  │ │ 5  │ │ 6  │ │ 7  │ │ 8  │ │ 9  │ │ 10 │
 │ 前 │ │ 函 │ │ 求 │ │ 后 │ │ 日 │ │ 监 │ │ 实 │
 │ 处 │ │ 数 │ │ 解 │ │ 处 │ │ 志 │ │ 控 │ │ 验 │
 │ 理 │ │ 模 │ │ 器 │ │ 理 │ │ 模 │ │ 模 │ │ 结 │
 │    │ │ 块 │ │    │ │    │ │ 块 │ │ 块 │ │ 果 │
 └────┘ └────┘ └────┘ └────┘ └────┘ └────┘ └────┘
```

### 1.2 模块职责一览

| # | 模块 | 职责 | 对标当前代码 |
|:---:|---|---|:---:|
| 1 | **实验模块** | 定义批量实验方案（YAML），管理实验列表和参数覆盖 | `0_experiments/run.cpp` |
| 2 | **启动器模块** | 程序入口、命令行参数、选择配置、触发编排 | `1_launcher/main.cpp` |
| 3 | **编排模块** ⭐ | 流程控制：初始化→运行→收尾，连接所有模块 | `ExperimentRunner.h` → **升级** |
| 4 | **前处理模块** | 数据生成/加载，构建问题实例 Instance | `DataGenerator.h` + `DataLoader.h` |
| 5 | **函数模块** | 纯数学公式（收益、成本、碳排放），无副作用 | `3_formula/` |
| 6 | **求解器模块** | GA+BP 算法，含可插拔策略（选择/交叉/变异/定价/分支） | `4_algorithm/` → **升级** |
| 7 | **后处理模块** | 计算被消掉的变量（Q_j, e+, e-），生成详细方案 | `5_postprocessor/` |
| 8 | **日志模块** | 运行时流水账，层级前缀缩进输出到终端+文件 | `Logger.h`（从 base 移出独立） |
| 9 | **监控模块** ⭐ | 运行时仪表盘，可查询的状态中心，供各模块上报和查询 | `Metrics.h` → **升级** |
| 10 | **实验结果模块** | 运行后的正式输出：收敛曲线、报告、实验对比表格 | `ResultWriter.h` → **升级** |

### 1.3 十大模块协作关系

下面按**运行时的调用顺序**展示十大模块如何协作：

#### 模式A：单次运行（Launcher → Orchestrator）

```
启动器模块 (Launcher)
  │  main_single.cpp：提供配置路径
  ▼
编排模块 (Orchestrator) ⭐
  │  生命周期：initialize() → run() → finalize()
  │
  ├── initialize():
  │     ├── 读配置（调用 10_common/Config.h）
  │     ├── 创建 日志模块 (Logger)    ← 设置输出文件
  │     ├── 创建 监控模块 (Monitor)   ← 重置计时
  │     ├── 调用 前处理模块 → 生成 Instance
  │     │     └── 内部使用 函数模块 计算距离、碳排放系数等
  │     ├── 创建 求解器模块（注入 Instance、Logger、Monitor）
  │     └── 创建 后处理模块
  │
  ├── run():
  │     ├── 求解器模块.GA.solve()
  │     │     ├── 记录日志 → 日志模块
  │     │     ├── 上报进度 → 监控模块
  │     │     ├── 调用 BP → 内部调用 列生成 → 定价子问题
  │     │     │     └── 定价子问题使用 函数模块 计算检验数
  │     │     └── 分支定界 → 返回最优解
  │     ├── 后处理模块.compute()
  │     │     └── 使用 函数模块 计算 Q_j, e+, e-
  │     └── 实验结果模块.collectFrom()
  │           ├── 从 监控模块 拿收敛曲线
  │           ├── 从 求解器模块 拿最优解
  │           └── 从 后处理模块 拿详细方案
  │
  └── finalize():
        ├── 关闭日志文件
        └── 输出运行摘要
```

#### 模式B：批量实验（Experiment → Launcher → Orchestrator）

```
实验模块 (Experiment)
  │  main_batch.cpp：加载实验方案 YAML
  │  ExperimentPlan：展开所有条目（每个条目 = 一份完整 Config）
  │  OutputManager：创建 results_experiment/时间戳_方案名/ 目录
  │
  ├── 对每个条目:
  │     ├── 启动器模块 (Launcher)：拿到 Config
  │     ├── 创建 编排模块 (Orchestrator)
  │     │     └── 走完整的 initialize → run → finalize
  │     └── 收集 ExperimentResult
  │
  └── 所有条目跑完后:
        └── 实验结果模块.saveExperimentTable()
              └── 输出 summary.txt（所有条目对比表格）
```

#### 模块依赖关系

```
依赖方向：A → B 表示 "A 调用 B"

实验模块 → 启动器模块 → 编排模块 ⭐
                          ├── 前处理模块 → 函数模块
                          ├── 求解器模块 → 函数模块
                          │               └── 可插拔策略
                          ├── 后处理模块 → 函数模块
                          ├── 日志模块
                          ├── 监控模块
                          └── 实验结果模块
```

**关键规则**：
1. **编排模块是唯一的大脑**：所有业务流程只在这里编排，其他模块没有自己的"主流程"
2. **函数模块是最底层的积木**：所有纯数学计算都在这里，其他模块只调用不重复实现
3. **日志和监控是运行时的左右手**：求解器只管上报，不关心输出格式
4. **实验结果模块是终局**：只在 run() 的最后阶段被收集，不参与运行中逻辑
5. **可插拔策略是求解器的内部扩展**：不影响到其他模块

---

## 二、核心三模块详解：日志、监控、实验结果

> **涉及模块**：#8 日志模块、#9 监控模块、#10 实验结果模块
> **协作位置**：这三个模块贯穿整个运行过程。日志和监控在运行中被各模块调用（写流水账、报状态），实验结果在运行结束后由编排模块统一收集并输出。它们是整个架构的"基础设施"，其他模块都依赖它们。

这是本次架构设计的核心——把当前混杂在一起的"输出"拆分为三个职责独立的模块。

### 2.1 日志模块 (Logger) — "流水账本"

**只管一件事**：把带层级、带前缀的信息一行行写出去。

```
[GA] 遗传算法第 0 代开始
  [BP] 评估个体 #0, w = [0.183, 0.189, 0.424]
    [CG] RMP迭代 #1
      [PS] j=0, p=60.4 → 最优S={0,1,3,4}，检验数=0.023
    [CG] 列生成收敛，LP最优解目标值=745169.0
  [BP] 个体 #0 适应度 = 745169.0
[GA] 第 0 代最优适应度 = 745169.0
```

**设计**：

```cpp
class Logger {
public:
    // 核心：按层级输出
    void log(int level, const std::string& msg);
    
    // 层级管理（进入/退出子模块时调用）
    void pushLevel();   // 缩进+1
    void popLevel();    // 缩进-1
    void setLevel(int level);  // 直接设置
    
    // 输出通道
    void setFileOutput(const std::string& path);
    void setConsoleOutput(bool enabled);
    void setVerbose(bool v);   // 详细模式（输出 DEBUG 信息）
    
    // 便捷方法
    void info(const std::string& msg);
    void debug(const std::string& msg);
    void warn(const std::string& msg);
    void error(const std::string& msg);
    void separator();
};
```

**关键设计决策**：
- Logger **不管内容是什么**，只管格式化 + 输出
- 层级前缀是 Logger 内部的事，调用者只需传 level
- 支持同时输出到终端 + 文件
- verbose 模式控制是否输出 DEBUG 信息

---

### 2.2 监控模块 (Monitor) — "仪表盘"

**管运行时的状态和进度**。它不是一个"记录器"，而是一个**可查询的状态中心**。所有模块都可以向它报告状态，也可以向它查询状态。

```
运行中 Monitor 内部维护的状态：

{
  phase: "GA搜索",              // 当前阶段
  generation: 5,                // 当前代数
  totalGenerations: 50,         // 总代数
  bestFitness: 608577.0,        // 当前最优
  avgFitness: 754199.0,         // 当前平均
  bestW: [0.35, 0.51, 0.37],   // 最优 w
  elapsed: 0.52s,               // 已耗时
  estimatedProgress: 0.12,      // 估算进度 12%
  branchNodes: 0,               // 分支节点数
  convergenceHistory: [         // 收敛历史
    {gen:0, best:550499, avg:550499, worst:550499},
    {gen:1, best:580000, avg:570000, worst:560000},
    ...
  ]
}
```

**设计**：

```cpp
class Monitor {
public:
    // ===== 各模块上报 =====
    void reportPhase(const std::string& phase);   // 阶段变更
    void reportGeneration(int gen, double best, double avg, 
                          double worst, const std::vector<double>& bestW);
    void reportIndividual(int idx, double fitness);
    void reportCGIteration(int iter, int numCols);
    void reportBBNodes(int total, int pruned);
    
    // ===== 查询接口 =====
    double bestFitness() const;
    double avgFitness() const;
    int currentGeneration() const;
    std::string currentPhase() const;
    double elapsedSeconds() const;
    double estimatedProgress() const;  // 0.0 ~ 1.0
    int etaSeconds() const;
    const std::vector<ConvergencePoint>& convergenceHistory() const;
    
    // ===== 时间管理 =====
    void reset();            // 重置计时
    void markPhaseStart();   // 标记阶段开始（用于分阶段计时）

private:
    // 内部状态（持续更新）
    std::string currentPhase_;
    int generation_ = 0;
    double bestFitness_ = -DBL_MAX;
    double avgFitness_ = 0.0;
    std::vector<double> bestW_;
    int totalBranchNodes_ = 0;
    std::vector<ConvergencePoint> convergenceHistory_;
    
    // 时间
    std::chrono::steady_clock::time_point startTime_;
    std::chrono::steady_clock::time_point phaseStartTime_;
};
```

**关键设计决策**：
- Monitor 是**可查询的**，不是只写的。其他模块可以随时 `monitor.bestFitness()` 获取当前最优值
- Monitor 是**状态累积的**，不是事件流。每代上报时会覆盖旧的最优值
- Monitor 负责估算剩余时间（根据已耗时代数和平均每代耗时）
- Monitor 只存关键摘要，不存每步细节（细节去查 Logger）

---

### 2.3 实验结果模块 (Result) — "成绩单"

**管运行结束后的一次性输出**。它不是运行中的东西，是跑完了生成的结果报告。

**设计**：

```cpp
// ===== 结果数据结构 =====
struct ExperimentResult {
    // 元信息
    std::string experimentName;
    int numDC;
    int numRetailers;
    double delta;
    int randomSeed;
    
    // 核心指标
    double totalProfit;
    double carbonEmission;
    double solveTime;
    int numDCsOpen;
    int numRtsServed;
    
    // 算法统计
    int totalBranchNodes;
    int totalCGIterations;
    double gaTime;
    double bpTime;
    
    // 最优解详情
    std::vector<double> bestW;
    std::vector<DCSolution> dcSolutions;
    CarbonResult carbonResult;
    
    // 收敛曲线
    std::vector<ConvergencePoint> convergence;
};

class ResultWriter {
public:
    // ===== 数据收集 =====
    // 从 Monitor 收集运行中积累的数据
    void collectFrom(const Monitor& monitor);
    // 从 Solver 收集最优解
    void collectFrom(const BranchAndPrice& bp);
    // 从 PostProcessor 收集后处理数据
    void collectFrom(const PostProcessor& postProc);
    
    // ===== 输出方法 =====
    // 单次运行的完整报告
    void saveReport(const std::string& path);
    // 收敛曲线 CSV
    void saveConvergenceCSV(const std::string& path);
    // 批量实验对比表格
    void saveExperimentTable(const std::string& path, 
                             const std::vector<ExperimentResult>& results);
    
    // ===== 便捷访问 =====
    const ExperimentResult& getResult() const { return result_; }
    
private:
    ExperimentResult result_;
};
```

**关键设计决策**：
- Result **不从 Logger 或 Monitor 继承数据**，而是通过 `collectFrom()` 主动收集
- 收敛曲线数据来自 Monitor（Monitor 记录了每代的点）
- 详细解方案来自 PostProcessor
- Result 可以独立存在，不依赖 Orchestrator 的生命周期

---

### 2.4 三个模块的关系图

```
运行中：

  求解器  ──reportGeneration()──▶  Monitor（仪表盘）
  求解器  ──log()───────────────▶  Logger（流水账）
     │                              │
     │                              ▼
     │                         终端 + 文件
     ▼
  Monitor 内部状态持续更新
  （可被随时查询）

运行后：

  Monitor ──collectFrom()──▶  ResultWriter（成绩单）
  Solver  ──collectFrom()──▶  ResultWriter
  PostProc──collectFrom()──▶  ResultWriter
                                 │
                                 ▼
                          report.txt + convergence.csv + experiments.txt
```

---

## 三、配置与实验体系

> **涉及模块**：#1 实验模块、#10 公共类型（Config.h）
> **协作位置**：这是整个项目的"入口层"。实验模块解析 YAML 方案，生成 Config 对象，交给启动器模块去运行。它不参与任何算法逻辑。

### 3.1 总体结构

```
配置和实验分两层：

src/
├── 1_launcher/
│   ├── main_single.cpp          ← 单次运行入口
│   └── configs/                 ← 第1层：单次运行配置文件（完整配置）
│       ├── delta3000.yaml
│       ├── delta5000.yaml
│       └── ...
│
├── 0_experiments/               ← 第2层：批量实验方案
│   ├── main_batch.cpp           ← 批量实验入口
│   ├── configs/                 ← 实验模块自己的配置子文件夹
│   │   ├── delta_scan.yaml
│   │   ├── table4_1.yaml
│   │   └── ...
│   ├── results_experiment/         ← 批量实验结果输出目录
│   │   ├── 20260719_153000_delta_scan/
│   │   ├── 20260719_160000_table4_1/
│   │   └── ...
│   └── ExperimentPlan.h
```

### 3.2 两种运行模式

#### 模式A：单次运行（Launcher 启动）

**入口**：`1_launcher/main_single.cpp`

**流程**：
```
main_single.cpp 写死的绝对路径
    ↓
引用 1_launcher/configs/ 下的某个完整配置文件
    ↓
创建 Orchestrator → initialize → run → finalize
    ↓
输出到 results_single/ 目录下，自动创建带时间戳的子文件夹
例如：results_single/20260719_153000_δ3000/
  ├── config_used.yaml     ← 本次实际使用的完整配置
  ├── run.log              ← 日志流水账
  ├── report.txt           ← 完整决策方案报告
  └── convergence.csv      ← 收敛曲线
```

**特点**：
- 每次运行独立子文件夹，不会覆盖上次结果
- 子文件夹命名：`时间戳_单次_配置名/`
- 适合调试、验证单个配置

```cpp
// main_single.cpp
//
// ===== 在这里改路径切换配置，每次启动都会自动创建新文件夹 =====
static const std::string CONFIG_PATH = 
    "/home/pei_zeyu/projects/cplex_code/4_重构代码/src/1_launcher/configs/delta3000.yaml";
// =============================================================

int main() {
    Orchestrator orchestrator;
    orchestrator.initialize(CONFIG_PATH);  // 内部自动创建 results_single/时间戳_配置名/ 目录
    orchestrator.run();
    orchestrator.finalize();
    return 0;
}
```

#### 模式B：批量实验（Experiment 启动）

**入口**：`0_experiments/main_batch.cpp`

**流程**：
```
main_batch.cpp 接收实验方案 YAML 路径
    ↓
引用 0_experiments/configs/ 下的某个实验方案
    ↓
实验方案通过 base_config 引用 1_launcher/configs/ 中的完整配置作为基础
    ↓
ExperimentPlan 展开所有实验条目，每个条目 = 完整 Config
    ↓
对每个 Config 创建 Orchestrator → 依次运行
    ↓
所有结果统一输出到 results_experiment/ 下
自动创建带时间戳和方案名的子文件夹
例如：results_experiment/20260719_160000_delta_scan/
  ├── config_used.yaml              ← 本次批量实验的完整方案配置
  ├── summary.txt                   ← 批量实验总览（含所有条目的对比表格）
  ├── delta1000/                    ← 每个实验条目一个子文件夹
  │   ├── config_used.yaml
  │   ├── run.log
  │   ├── report.txt
  │   └── convergence.csv
  ├── delta3000/
  │   ├── config_used.yaml
  │   ├── run.log
  │   ├── report.txt
  │   └── convergence.csv
  ├── delta5000/
  │   └── ...
  └── ...
```

**特点**：
- 每个条目的输出和单次运行一样完整（report.txt + convergence.csv + run.log）
- 额外多一个 `summary.txt`：把所有条目汇总成一个对比表格
- 子文件夹命名：`时间戳_方案名/`
- 适合批量跑实验

### 3.3 实验方案文件格式

实验方案放在 `0_experiments/configs/` 下：

```yaml
# 0_experiments/configs/delta_scan.yaml

# ===== 引用基础配置 =====
base_config: /home/pei_zeyu/projects/cplex_code/4_重构代码/src/1_launcher/configs/delta3000.yaml

# ===== 元信息 =====
plan_name: "delta_scan"
plan_desc: "固定其他参数为基准值，扫描delta从1000到10000"

# ===== 顶层全局覆盖（可选，所有条目共享）=====
# （此处不写任何字段，全部继承自 base_config）

# ===== 实验列表 =====
experiments:
  - name: "delta1000"
    delta: 1000
  - name: "delta3000"
    delta: 3000
    random_seed: 99999          # 可选：单独覆盖种子
  - name: "delta5000"
    delta: 5000
  - name: "delta7000"
    delta: 7000
  - name: "delta10000"
    delta: 10000
```

### 3.4 输出目录规则

| 启动方式 | 根输出目录 | 子文件夹命名 | 包含内容 |
|---|---|---|---|
| 单次运行 | `results_single/` | `YYYYMMDD_HHmmss_配置名/` | config_used.yaml, run.log, report.txt, convergence.csv |
| 批量实验 | `results_experiment/` | `YYYYMMDD_HHmmss_方案名/` | summary.txt + 每个条目一个子文件夹（同单次） |

### 3.5 运行模式（run_mode）

Config 新增 `run_mode` 字段，控制编排模块的求解路径：

| 模式 | 值 | 说明 |
|---|---|---|
| 标准模式 | `"ga"`（默认） | 完整 GA+BP：GA 搜索 w，BP 对每个 w 精确求解 |
| 固定 w 模式 | `"fixed_w"` | 跳过 GA，只跑一次 BP。w 值由 `fixed_w` 字段指定（如 `fixed_w: [0, 0, 0]`） |

`run_mode` 是可被实验条目覆盖的字段，遵守配置优先级规则。
当 `run_mode: "fixed_w"` 时，编排模块的 run() 流程变为：

```
run():
  ├── 阶段1: BP.solve(fixed_w)   ← 直接调用 BP，不走 GA
  ├── 阶段2: 后处理
  └── 阶段3: 输出结果
```

### 3.6 配置优先级

```
实验条目中覆盖的字段  >  实验方案顶层字段  >  base_config 中的字段  >  Config 类默认值
```

### 3.7 关键类设计

```cpp
// ===== 实验方案解析 =====
class ExperimentPlan {
public:
    bool load(const std::string& yamlPath);
    
    // 展开所有实验条目，每个条目是一份完整 Config
    std::vector<Config> expand() const;
    
    // 元信息
    std::string planName() const;
    std::string planDesc() const;
    
private:
    std::string baseConfigPath_;
    Config baseConfig_;
    std::vector<ExperimentEntry> entries_;
};

// ===== 输出目录管理器 =====
class OutputManager {
public:
    // 为单次运行创建输出目录
    // 返回：results_single/20260719_153000_δ3000/
    std::string createSingleRunDir(const Config& config);
    
	    // 为批量实验创建输出目录
    // 返回：results_experiment/20260719_160000_delta_scan/
    std::string createExperimentDir(const std::string& planName);
    
    // 在批量实验的目录下为单个条目创建子目录
    // 返回：.../delta1000/
    std::string createEntryDir(const std::string& parentDir, 
                               const std::string& entryName);
    
private:
    static std::string timestamp();
    // 格式：20260719_153000
};
```

---

## 四、编排模块详解 (Orchestrator)

> **涉及模块**：#3 编排模块 ⭐
> **协作位置**：编排模块是十大模块的"大脑"。它不自己做数学运算，而是协调其他模块按流程工作。initialize() 时组装好其他模块，run() 时按顺序调用求解器→后处理→实验结果。

### 4.1 三阶段生命周期

```
┌─────────────────────────────────────────────────────────────────────┐
│ Orchestrator                                                         │
│                                                                      │
│  1. initialize(configPath)                                            │
│     ├── 读配置 → Config                                               │
│     ├── 创建 Logger（设置输出文件、verbose 模式）                      │
│     ├── 创建 Monitor（重置计时）                                      │
│     ├── 前处理 → 生成/加载 Instance                                   │
│     ├── 创建求解器（注入 Instance、Logger、Monitor）                   │
│     └── 创建后处理器                                                  │
│                                                                      │
│  2. run()                                                             │
│     ├── 阶段1: GA 搜索 w（第4层）                                      │
│     │     ├── Logger.log("遗传算法开始")                               │
│     │     ├── 初始化种群（二进制编码 w_j）                              │
│     │     ├── for gen = 0..maxGen:                                    │
│     │     │     ├── Logger.pushLevel()                                │
│     │     │     ├── for each 个体 i:                                  │
│     │     │     │     ├── 解码 w = binaryToDecimal(chrom)             │
│     │     │     │     ├── 适应度 = BP.solve(w)          ← 进入第3层  │
│     │     │     │     └── Monitor.reportIndividual(i, fitness)        │
│     │     │     ├── Monitor.reportGeneration(gen, best, avg, worst)   │
│     │     │     ├── 选择（轮盘赌）                                     │
│     │     │     ├── 交叉（两点交叉）                                   │
│     │     │     ├── 变异（按位翻转）                                   │
│     │     │     ├── 精英保留                                          │
│     │     │     ├── Logger.popLevel()                                 │
│     │     │     └── Logger.log("第X代最优=xxx")                       │
│     │     └── Logger.log("GA收敛，最优w=xxx")                         │
│     │                                                                 │
│     │              ┌──────────────────────────────────────────────┐   │
│     │              │ BP.solve(w) 内部（第3层：分支定价）            │   │
│     │              │                                              │   │
│     │              │  1. 初始化RMP（初始列）                       │   │
│     │              │  2. CG.solve()  ← 进入第2层                   │   │
│     │              │     ┌──────────────────────────────┐         │   │
│     │              │     │ 列生成（第2层）                │         │   │
│     │              │     │ repeat:                       │         │   │
│     │              │     │   求解RMP → 对偶变量 λ         │         │   │
│     │              │     │   PS.solve()  ← 进入第1层     │         │   │
│     │              │     │     ┌──────────────────┐     │         │   │
│     │              │     │     │ 定价子问题（第1层）│     │         │   │
│     │              │     │     │ for j in DCs:     │     │         │   │
│     │              │     │     │   for p in prices:│     │         │   │
│     │              │     │     │     求最优 S       │     │         │   │
│     │              │     │     │     算检验数 RC    │     │         │   │
│     │              │     │     │ 取全局最优列       │     │         │   │
│     │              │     │     └──────────────────┘     │         │   │
│     │              │     │   if RC > 0: 加列到RMP      │         │   │
│     │              │     │   until RC <= 0              │         │   │
│     │              │     └──────────────────────────────┘         │   │
│     │              │  3. BB.solve() ← 分支定界修复整数性           │   │
│     │              │     对分数变量分支（DFS）                     │   │
│     │              │  4. 返回整数最优解（或LP解）                   │   │
│     │              └──────────────────────────────────────────────┘   │
│     │                                                                 │
│     ├── 阶段2: 后处理                                                 │
│     │     ├── 计算 D_j（各DC服务总需求）                               │
│     │     ├── 计算 Q_j*（EOQ最优订货量）                              │
│     │     ├── 计算 E（总碳排放）                                      │
│     │     ├── 计算 e+ / e-（碳交易量）                                │
│     │     └── Logger.log("后处理完成")                                │
│     │                                                                 │
│     └── 阶段3: 输出结果                                                │
│           ├── result.collectFrom(monitor, solver, postProc)           │
│           ├── result.saveReport(reportPath)                           │
│           └── result.saveConvergenceCSV(csvPath)                      │
│                                                                      │
│  3. finalize()                                                        │
│     ├── Logger.log("运行完成")                                        │
│     ├── 关闭 Logger 文件                                              │
│     └── 输出摘要到终端                                                │
└─────────────────────────────────────────────────────────────────────┘
```

**关于基准方案（w=0）**：基准方案不属于编排流程的一部分。它应该作为**批量实验的一个条目**来执行。
例如，在实验方案中定义两个条目：

```yaml
experiments:
  - name: "不减排基准"     # 条目1：固定 w=0（不走 GA，只跑 BP）
    run_mode: "fixed_w"    # 新增 Config 字段，见下文说明
  - name: "GA优化减排"     # 条目2：GA 自动搜索最优 w
    # 使用 config 中的默认 GA 参数
```

两个条目各自走完整的"初始化→求解→后处理→输出"流程，然后在 summary.txt 中对比结果。
这样基准和优化方案的对比逻辑在**实验模块**层面，而不是编排层面。

### 4.2 接口设计

```cpp
class Orchestrator {
public:
    Orchestrator();
    ~Orchestrator();
    
    // 三阶段生命周期
    void initialize(const std::string& configPath);
    void run();
    void finalize();
    
    // 快捷方式：一步完成所有
    void runAll(const std::string& configPath);
    
    // 访问内部组件（供外部读取结果）
    const Monitor& getMonitor() const;
    const ExperimentResult& getResult() const;
    
private:
    // 核心组件
    Config config_;
    Logger logger_;
    Monitor monitor_;
    ResultWriter resultWriter_;
    
    // 模块
    std::unique_ptr<Instance> instance_;
    std::unique_ptr<GeneticAlgorithm> ga_;
    std::unique_ptr<BranchAndPrice> bp_;
    std::unique_ptr<PostProcessor> postProc_;
    
    // 内部流程
    void runGA();
    void runPostProcess();
    void runOutput();
};
```

---

## 五、求解器模块 + 可插拔策略

> **涉及模块**：#6 求解器模块
> **协作位置**：求解器是编排模块最核心的调用对象。它内部包含四层嵌套（GA→BP→CG→PS），每层都通过日志模块记录流水账、通过监控模块上报进度。它依赖函数模块做所有数学计算。

### 5.1 策略体系

```
Solver 模块内部结构：

GeneticAlgorithm
├── SelectionStrategy (接口)
│   ├── RouletteSelection     ← 轮盘赌（默认）
│   ├── TournamentSelection   ← 锦标赛
│   └── RankSelection         ← 排名选择
├── CrossoverStrategy (接口)
│   ├── TwoPointCrossover     ← 两点交叉（默认）
│   ├── UniformCrossover      ← 均匀交叉
│   └── SinglePointCrossover  ← 单点交叉
├── MutationStrategy (接口)
│   ├── BitFlipMutation       ← 按位翻转（默认）
│   ├── AdaptiveMutation      ← 自适应变异
│   └── GaussianMutation      ← 高斯变异
└── uses → BranchAndPrice

BranchAndPrice
├── uses → ColumnGeneration
│   └── uses → PricingStrategy (接口)
│       ├── SimplifiedConvexHull  ← 简化凸包枚举（默认）
│       └── PaperAlgorithm3       ← 论文 Algorithm 3
└── uses → BranchAndBound
    └── uses → BranchStrategy (接口)
        ├── MostFractionalBranch  ← 最分数变量分支（默认）
        └── PseudoCostBranch      ← 伪代价分支
```

### 5.2 策略接口设计

```cpp
// ===== 选择策略 =====
class SelectionStrategy {
public:
    virtual ~SelectionStrategy() = default;
    virtual std::vector<std::vector<double>> select(
        const std::vector<std::vector<double>>& population,
        const std::vector<double>& fitness,
        Random& rng) = 0;
    virtual std::string name() const = 0;
};

// ===== 交叉策略 =====
class CrossoverStrategy {
public:
    virtual ~CrossoverStrategy() = default;
    virtual std::vector<std::vector<double>> crossover(
        const std::vector<std::vector<double>>& parents,
        double crossoverRate,
        Random& rng) = 0;
    virtual std::string name() const = 0;
};

// ===== 变异策略 =====
class MutationStrategy {
public:
    virtual ~MutationStrategy() = default;
    virtual void mutate(
        std::vector<std::vector<double>>& offspring,
        double mutationRate,
        Random& rng) = 0;
    virtual std::string name() const = 0;
};

// ===== 定价策略 =====
class PricingStrategy {
public:
    virtual ~PricingStrategy() = default;
    virtual PricingResult solve(int j, double p_j,
                                const std::vector<double>& dual,
                                const Instance& inst) = 0;
    virtual std::string name() const = 0;
};

// ===== 分支策略 =====
class BranchStrategy {
public:
    virtual ~BranchStrategy() = default;
    virtual BranchState selectBranch(
        const std::vector<std::vector<double>>& z_j_values) = 0;
    virtual std::string name() const = 0;
};
```

### 5.3 策略工厂

```cpp
class StrategyFactory {
public:
    static std::unique_ptr<SelectionStrategy> createSelection(
        const std::string& name);
    static std::unique_ptr<CrossoverStrategy> createCrossover(
        const std::string& name);
    static std::unique_ptr<MutationStrategy> createMutation(
        const std::string& name);
    static std::unique_ptr<PricingStrategy> createPricing(
        const std::string& name);
    static std::unique_ptr<BranchStrategy> createBranch(
        const std::string& name);
};
```

通过 Config 选择：
```yaml
solver:
  selection: "tournament"       # roulette / tournament / rank
  crossover: "uniform"          # two_point / uniform / single_point
  mutation: "adaptive"          # bit_flip / adaptive / gaussian
  pricing: "simplified"         # simplified / algorithm3
  branching: "most_fractional"  # most_fractional / pseudo_cost
```

---

## 六、启动器模块 (Launcher) + 实验模块 (Experiment)

> **涉及模块**：#1 实验模块、#2 启动器模块
> **协作位置**：它们都在编排模块的上层。启动器是程序的"大门"，接收外部命令（改个路径或传个参数），创建编排者并触发运行。实验模块是批量实验的"调度者"，把实验方案展开成多个 Config，对每个 Config 创建启动器来执行。

### 6.1 单次运行启动器（Launcher）

```cpp
// 1_launcher/main_single.cpp
//
// ===== 在这里改路径切换配置 =====
static const std::string CONFIG_PATH = 
    "/home/pei_zeyu/projects/cplex_code/4_重构代码/src/1_launcher/configs/delta3000.yaml";
// ================================

int main() {
    Orchestrator orchestrator;
    orchestrator.initialize(CONFIG_PATH);
    orchestrator.run();
    orchestrator.finalize();
    return 0;
}
```

启动后自动创建输出目录：
```
results_single/
└── 20260719_153000_δ3000/
    ├── config_used.yaml
    ├── run.log
    ├── report.txt
    └── convergence.csv
```

### 6.2 批量实验启动器（Experiment）

```cpp
// 0_experiments/main_batch.cpp

int main(int argc, char** argv) {
    // 用法：./bin/batch [实验方案名]
    // 例如：./bin/batch delta_scan
    //       ./bin/batch table4_1
    std::string planName = "table4_1";
    if (argc >= 2) planName = argv[1];
    
    std::string planPath = "src/0_experiments/configs/" + planName + ".yaml";
    
    // 1. 加载实验方案
    ExperimentPlan plan;
    if (!plan.load(planPath)) {
        std::cerr << "无法加载实验方案: " << planPath << std::endl;
        return 1;
    }
    
    // 2. 创建实验输出目录
    OutputManager outMgr;
    std::string expDir = outMgr.createExperimentDir(plan.planName());
    // 例如：results_experiment/20260719_160000_delta_scan/
    
    // 3. 保存本次实验的方案配置
    plan.saveConfigTo(expDir + "/config_used.yaml");
    
    // 4. 展开所有实验条目
    std::vector<Config> configs = plan.expand();
    
    // 5. 依次运行每个条目
    std::vector<ExperimentResult> allResults;
    for (size_t i = 0; i < configs.size(); i++) {
        Config& config = configs[i];
        std::string entryName = plan.entryName(i);
        
        // 为每个条目创建独立子目录
        std::string entryDir = outMgr.createEntryDir(expDir, entryName);
        // 例如：results_experiment/20260719_160000_delta_scan/delta1000/
        
        // 设置输出路径
        config.output_dir = entryDir;
        config.log_file = entryDir + "/run.log";
        
        // 运行
        Orchestrator orchestrator;
        orchestrator.initialize(config);
        orchestrator.run();
        orchestrator.finalize();
        
        allResults.push_back(orchestrator.getResult());
    }
    
    // 6. 输出汇总对比表格
    ResultWriter writer;
    writer.saveExperimentTable(expDir + "/summary.txt", allResults);
    
    std::cout << "\n实验结果已保存至: " << expDir << std::endl;
    return 0;
}
```

启动后自动创建输出目录：
```
results_experiment/
└── 20260719_160000_delta_scan/
    ├── config_used.yaml              ← 本次批量实验的完整方案
    ├── summary.txt                   ← 所有条目的对比表格
    ├── delta1000/                    ← 条目1：单次结果
    │   ├── config_used.yaml
    │   ├── run.log
    │   ├── report.txt
    │   └── convergence.csv
    ├── delta3000/                    ← 条目2
    │   └── ...
    ├── delta5000/                    ← 条目3
    │   └── ...
    └── ...
```

---

## 七、前处理模块 (Preprocessor)

> **涉及模块**：#4 前处理模块
> **协作位置**：在编排模块的 initialize() 阶段被调用。它把 Config 转化为 Instance——求解器需要的"原材料"。内部通过 DataSourceStrategy 可插拔策略支持随机生成、文件加载、legacy兼容三种数据来源。

### 7.1 数据源策略

```cpp
class DataSourceStrategy {
public:
    virtual ~DataSourceStrategy() = default;
    virtual Instance generate(const Config& config) = 0;
    virtual std::string name() const = 0;
};

class RandomGenerationStrategy : public DataSourceStrategy {
    // 按论文规则随机生成
};

class FileLoadStrategy : public DataSourceStrategy {
    // 从文件加载已有数据（复现用）
};

class LegacyCompatibleStrategy : public DataSourceStrategy {
    // 模拟原代码的数据生成方式
};
```

---

## 八、函数模块 (Formula) — 保持不动

> **涉及模块**：#5 函数模块
> **协作位置**：函数模块是十大模块中最底层的"积木"。它不依赖任何其他模块，但被前处理、求解器、后处理三个模块广泛调用。所有纯数学公式（收益、成本、碳排放）都在这里，确保一处实现、处处一致。

当前 `3_formula/` 设计已经很好了，纯函数、可独立测试。

| 文件 | 公式 | 说明 |
|:---|---:|:---|
| 01_Revenue | R = p_j × μ_i | 收益 |
| 02_FacilityCost | f_j + F_j + g_j | 设施固定成本 |
| 03_TransportCost | c(a_j)×D_j + Σc(d_ij)×μ_i | 运输成本（分段函数） |
| 04_InventoryCost | EOQ + 安全库存 | 库存成本 |
| 05_InvestmentCost | ½ × δ × w² × √D | 减排投资 |
| 06_CarbonCredit | p̂ × (E - C) | 碳交易 |
| 07_Constraints | 约束检查 | 可行性验证 |
| 08_Objective | 总目标函数聚合 | 利润 = 收益 - 所有成本 |
| 09_PostCompute | 被消掉变量的计算 | Q_j, e+, e- |

---

## 九、后处理模块 (Postprocessor) — 保持不动

> **涉及模块**：#7 后处理模块
> **协作位置**：在编排模块 run() 的"阶段2"被调用。求解器给出最优解后，后处理负责计算被消掉的变量（Q_j、e+、e-），生成详细方案。它的结果最终被实验结果模块收集。

当前 `5_postprocessor/` 设计合理，保持：

- `PostProcessor`：计算 Q_j, D_j, e+, e-, 碳排放详情
- `ResultWriter`：生成文本报告（已有部分功能，未来迁移到新的 ResultWriter）

---

## 十、目录结构（建议）

```
4_重构代码/
├── Makefile
│
├── results_single/                   ← 单次运行结果（Launcher 自动创建子文件夹）
│   └── 20260719_153000_δ3000/
│       ├── config_used.yaml
│       ├── run.log
│       ├── report.txt
│       └── convergence.csv
│
├── results_experiment/               ← 批量实验结果（Experiment 自动创建子文件夹）
│   └── 20260719_160000_delta_scan/
│       ├── config_used.yaml
│       ├── summary.txt
│       ├── delta1000/
│       │   ├── config_used.yaml
│       │   ├── run.log
│       │   ├── report.txt
│       │   └── convergence.csv
│       ├── delta3000/
│       └── ...
│
├── src/
│   ├── 0_experiments/                ← 实验模块
│   │   ├── main_batch.cpp            ← 批量实验入口（./bin/batch 方案名）
│   │   ├── ExperimentPlan.h          ← 实验方案解析（支持 base_config）
│   │   └── configs/                  ← 实验模块的配置子文件夹
│   │       ├── delta_scan.yaml
│   │       ├── carbon_price_sweep.yaml
│   │       ├── scalability_test.yaml
│   │       ├── table4_1.yaml
│   │       ├── table4_2.yaml
│   │       └── ...
│   │
│   ├── 1_launcher/                   ← 启动器模块
│   │   ├── main_single.cpp           ← 单次运行入口（绝对路径引用配置）
│   │   ├── Launcher.h                ← 启动器工具
│   │   └── configs/                  ← 第1层：单次运行配置文件
│   │       ├── delta3000.yaml        ← 基准配置
│   │       ├── delta5000.yaml        ← 论文原值
│   │       ├── delta3000_5x10.yaml
│   │       └── ...
│   │
│   ├── 2_orchestrator/               ← 编排模块 ⭐
│   │   ├── Orchestrator.h            ← 流程编排者（三阶段生命周期）
│   │   └── StrategyFactory.h         ← 策略工厂
│   │
│   ├── 3_monitor/                    ← 监控模块 ⭐（运行时仪表盘）
│   │   └── Monitor.h
│   │
│   ├── 4_logger/                     ← 日志模块（流水账本）
│   │   └── Logger.h
│   │
│   ├── 5_result/                     ← 实验结果模块（成绩单）
│   │   ├── ExperimentResult.h        ← 结果数据结构
│   │   └── ResultWriter.h            ← 结果输出（报告/CSV/表格）
│   │
│   ├── 6_preprocessor/               ← 前处理模块
│   │   ├── Instance.h
│   │   ├── DataGenerator.h
│   │   ├── DataLoader.h
│   │   └── strategies/
│   │       ├── DataSourceStrategy.h
│   │       ├── RandomGeneration.h
│   │       └── FileLoad.h
│   │
│   ├── 7_formula/                    ← 函数模块（保持不变）
│   │   ├── 01_Revenue.h
│   │   ├── 02_FacilityCost.h
│   │   ├── 03_TransportCost.h
│   │   ├── 04_InventoryCost.h
│   │   ├── 05_InvestmentCost.h
│   │   ├── 06_CarbonCredit.h
│   │   ├── 07_Constraints.h
│   │   ├── 08_Objective.h
│   │   └── 09_PostCompute.h
│   │
│   ├── 8_solver/                     ← 求解器模块
│   │   ├── GeneticAlgorithm.h
│   │   ├── BranchAndPrice.h
│   │   ├── ColumnGeneration.h
│   │   ├── PricingSolver.h
│   │   ├── BranchAndBound.h
│   │   ├── CplexWrapper.h
│   │   └── strategies/
│   │       ├── SelectionStrategy.h
│   │       ├── RouletteSelection.h
│   │       ├── TournamentSelection.h
│   │       ├── CrossoverStrategy.h
│   │       ├── MutationStrategy.h
│   │       ├── PricingStrategy.h
│   │       └── BranchStrategy.h
│   │
│   ├── 9_postprocessor/              ← 后处理模块（保持不变）
│   │   ├── PostProcessor.h
│   │   └── ResultWriter.h
│   │
│   ├── 10_common/                    ← 公共类型和工具
│   │   ├── Types.h
│   │   ├── Solution.h
│   │   ├── Random.h
│   │   ├── ThreadPool.h
│   │   ├── Config.h                  ← 配置类
│   │   └── OutputManager.h           ← 输出目录管理器
│   │
│   └── 99_test/
│       ├── test_formula.cpp
│       └── test_pricing.cpp
│
└── Build.mk
```

---

## 十一、实施建议

### 11.1 分阶段实施

| 阶段 | 内容 | 涉及文件 |
|:---:|---|---|
| **1** | **配置体系 + 输出目录管理** | 新建 `1_launcher/configs/`，迁移配置文件；新建 `ExperimentPlan.h`（支持 `base_config`）；`10_common/` 下新建 `OutputManager.h`（时间戳子文件夹） |
| **2** | **Logger 独立 + Monitor 新建** | 把 Logger 从 base 移出独立；新建 Monitor 类；替换各模块中的 cout |
| **3** | **Result 模块** | 新建 `ExperimentResult.h` + `ResultWriter.h`，整合现有的结果输出；支持单次 report 和批量 summary |
| **4** | **Orchestrator** | 从 ExperimentRunner 改造为三阶段生命周期的 Orchestrator；集成 OutputManager 自动创建输出目录 |
| **5** | **实验模块启动器** | 改造 `main_batch.cpp`，使用 ExperimentPlan + OutputManager；支持 `./bin/batch 方案名` |
| **6** | **策略接口抽取** | GA 算子、定价、分支改为策略模式 |
| **7** | **目录结构调整** | 按新目录组织文件，更新 Makefile 和 Build.mk |
| **8** | **测试验证** | 确保单次运行和批量实验的结果与当前代码一致 |

### 11.2 可以不动的内容

| 模块 | 原因 |
|---|---|
| `7_formula/` | 纯函数，接口清晰，无需改动 |
| `9_postprocessor/` | 职责单一，设计合理 |
| `10_common/Types.h` | 基础类型定义 |
| `10_common/Random.h` | 简单且稳定 |
| 算法核心逻辑 | GA/BP/CG/PS/BB 的数学逻辑不变，只重构接口 |
