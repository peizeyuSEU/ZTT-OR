# 上下文 — 项目状态与动态追踪

## 当前项目状态（2026-07-13）

### ✅ 已完成

1. **原始代码理解** — 阅读并理解了 GA_BP.cpp（1075行）的全部逻辑
2. **模块化重构** — 将单文件拆分为独立模块
3. **配置管理** — 实现 config.yaml 读取，所有参数可配无需重新编译
4. **数据生成** — 支持 standard（论文参数）和 legacy（原代码兼容）两种模式
5. **四层算法** — GeneticAlgorithm → BranchAndPrice → ColumnGeneration → PricingSolver 完整实现
6. **分支定界剪枝逻辑修复** — 正确维护全局上下界
7. **层级日志** — [GA][BP][CG][BB][PS] 五级日志
8. **后处理模块** — 计算 Q_j*、D_j、E、e+、e-、净碳交易成本
9. **结果报告** — 生成完整决策方案报告
10. **收敛曲线** — CSV格式输出
11. **文件分类整理** — 0~4 + .ai + bin
12. **公式层独立** — formula/01~09，纯函数，可独立验证
13. **公式修正** — h=1→h=10，投资成本加√D_j，用delta代替beta
14. **公式验证测试** — test_formula.cpp 编译通过并运行
15. **算法层已改为调 formula** — PricingSolver, ColumnGeneration, BranchAndPrice, BranchAndBound, PostProcessor
16. **删除旧 model/Objective.h** — 功能移至 formula/08_Objective.h
17. **6层架构代码实现完成**：
    - 第0层 0_experiments/ 已建，含3个YAML实验方案（table4_1/4_2/verify）
    - 第1层 1_launcher/ 已建，含 main.cpp, ExperimentRunner.h
    - 基础层 0_base/ 已建，合并 DataGenerator/DataLoader + 基础工具
    - 删除旧的 utils/, solvers/, 以及 src/ 根目录的旧入口文件
    - Makefile 已适配新目录结构
18. **编译通过** — ga_bp, run, test_formula 全部可正常编译运行
19. **配置文件统一** — 将 JSON+YAML 两套制改为单一 YAML：
    - Config.h 新增 ExperimentEntry 结构体和 experiments 列表解析
    - 重写 YAML 解析器（支持缩进、列表、experiments section）
    - 每个实验条目可独立覆盖任意 Config 字段（不限于 num_dc/num_retailers/delta）
20. **实验运行器重写** — run.cpp 去掉 JSON 解析器，直接使用 Config.experiments
21. **路径修复** — 根目录 Makefile + config.yaml 到位，运行路径统一
22. **DataGenerator/DataLoader 移动** — 从 1_launcher/ 移到 0_base/
23. **旧 JSON 删除** — 3个 JSON 实验方案文件已删除
24. **文件编号统一** — 所有代码目录按编号体系整理（algorithm 01~05，postprocessor 01~02，0_base 拆为01_types/02_data/03_utils/04_solvers）
25. **公式一致性审计完成** — 逐项对比代码公式与论文公式，确认所有核心公式一致
26. **投资成本公式修复** — GeneticAlgorithm.h 的 solveWithDetailedResult 改为调用公式层 `InvestmentCostFormula::compute()` 使用 `½δw²√D_j` 论文标准版
27. **论文 Algorithm 3 实现** — 完整的参数线性规划凸包算法（余弦相似度、I1/I2/I3分类、12候选集），配置开关 `use_paper_algorithm3`
28. **Algorithm 3 验证通过** — 对比测试 30/30 全部一致，简化版和 Algorithm 3 结果一致（最大差值 0.0）
29. **Bug 修复** — BranchAndPrice 中投资成本公式开关未读取 config 的问题已修复；BranchAndBound 中 rc_eps 未从 config 传递的问题已修复

### ⚠️ 当前已知问题

1. **未验证论文结果** — 尚未运行对比论文表4.1的结果（最重要）
2. **分支定界节点数未验证** — 未实际测试
3. **性能未评估** — 60秒内跑完50代是否可行未确认
### 📋 下一步计划

**优先级1：运行验证**
- [ ] #DCs=3, #Rts=5 跑一次，对比论文表4.1
- [ ] #DCs=3, #Rts=10 跑一次，对比论文表4.1的 745,169.0
- [ ] 确认分支定界节点数 < 1000
- [ ] 确认求解时间

**优先级2：性能优化**
- [ ] 如求解时间超60秒，优化缓存和定价子问题

### 审稿状态

- 期刊: Operational Research (ORIJ-D-26-00062)
- 一审结果: 大修（Major Revision）
- 截止日期: 2026年8月10日
- 审稿人: 4位
- 核心修改要求: 基准测试对比、算法可复现性、参数依据说明、大规模测试、管理启示加强
