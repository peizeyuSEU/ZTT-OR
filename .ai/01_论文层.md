# 论文层 — 论文与代码的对应

## 论文基本信息

- **标题**: When Carbon Meets Capital: Optimizing Supply Chain Network Design with Technology Investment under Cap-and-Trade Policy
- **期刊**: Operational Research (ORIJ-D-26-00062)
- **状态**: 一审大修中（2026年8月10日截止）
- **投稿论文**: `1投稿论文latex/Manuscript.tex`
- **硕士原文**: `0硕士原版论文/小论文中文内容.docx`
- **审稿意见**: `2论文返修意见/本篇评审意见.docx`

## 核心问题

碳限额与交易政策下，企业同时决策：
1. **选址** — 在哪建配送中心（DC）
2. **分配** — 哪些零售商分配给哪个DC
3. **定价** — 每个DC定什么批发价（通过价格筛选有利可图的零售商）
4. **库存** — 每个DC的订货量 Q_j（EOQ公式消掉）
5. **碳交易** — 买/卖多少碳配额（e+, e-）
6. **减排投资** — 每个DC的减排率 w_j ∈ [0,1]

目标：**利润最大化**

## CAT-SCND 数学模型

### 原始模型 Model-Πκ（论文式3.54-3.58）

目标函数：
```
max Σ_j[ Σ_i r_i(p_j)μ_iY_ij - f_jX_j - ½δw²_j√(Σ_iμ_iY_ij)
    - ((F_j+g_j)/Q_j + c(a_j) + c(d_ij))Σ_iμ_iY_ij
    - hQ_j/2 - hz_α√(L_jΣ_iσ²_iY_ij) ] - p̂(e⁺-e⁻)
```

约束：
- 碳约束：总碳排放(1-w_j) + (e⁻-e⁺) = C
- 每个零售商最多由一个DC服务
- Y_ij ≤ X_j（只有开设的DC能服务）
- 0 ≤ w_j ≤ 1, X_j,Y_ij ∈ {0,1}

### 重写后的模型 Model-Πχ（论文式3.62-3.69）

通过EOQ公式消掉Q_j，通过碳约束消掉e+,e-：
```
max Σ_j[ Σ_i r_i(p_j)μ_iY_ij - (f_j + p̂f̂_j(1-w_j))X_j
    - Σ_i(c(d_ij) + p̂b̂_ij(1-w_j))μ_iY_ij
    - ½δw²_j√(Σ_iμ_iY_ij)
    - (h + p̂ĥ(1-w_j))z_α√(L_jΣ_iσ²_iY_ij)
    - √(2(F_j+g_j)(h + p̂ĥ(1-w_j))Σ_iμ_iY_ij)
    - (c(a_j) + p̂b̂_j(1-w_j))Σ_iμ_iY_ij ] + p̂C
```

### 集合覆盖模型 Model-ΠM（论文式4.3-4.6）

对固定 w_j：
```
max Σ_jΣ_{S⊆I} R̄_{j,S} Z̄_{j,S} + p̂C
s.t. Σ_jΣ_{S: i∈S} Z̄_{j,S} ≤ 1, ∀i
     Σ_{S⊆I} Z̄_{j,S} ≤ 1, ∀j
     Z̄_{j,S} ∈ {0,1}
```

其中 R̄_{j,S} 为DC j服务集合S的净收益：
```
R̄_{j,S} = Σ_i r_i(p_j)μ_i - (f_j + p̂f̂_j(1-w_j))
         - Σ_i(c(d_ij) + p̂b̂_ij(1-w_j))μ_i
         - ½δw²_j√(Σ_iμ_i)
         - (h + p̂ĥ(1-w_j))z_α√(L_jΣ_iσ²_i)
         - √(2(F_j+g_j)(h + p̂ĥ(1-w_j))Σ_iμ_i)
         - (c(a_j) + p̂b̂_j(1-w_j))Σ_iμ_i
```

## 四层求解框架

| 层级 | 作用 | 方法 | 对应代码 |
|:---|:---|:---|:---|
| 第1层 | 消掉库存变量 | EOQ公式 Q_j* = √(2(F_j+g_j)D_j / (h + p̂ĥ(1-w_j))) | Objective.h |
| 第2层 | 求解LP松弛 | **列生成**（含定价子问题，参数线性规划 O(n³)） | ColumnGeneration.h → PricingSolver.h |
| 第3层 | 修复整数性 | **分支定界**（DFS，对分数 Z_{j,S} 分支为0或1） | BranchAndBound.h |
| 第4层 | 搜索 w_j | **遗传算法**（二进制编码10位，精英保留，轮盘赌） | GeneticAlgorithm.h |

### 完整求解链
```
遗传算法(GA) → 搜索w_j
  ↓ 对每个个体(一组w_j)
  分支定价(BP) → 固定w_j求解整数规划
    ↓
    列生成(CG) → 求解LP松弛
      ↓
      定价子问题(PS) → 找使检验数最大的(j,S,p_j)
    ↓
    分支定界(BB) → 修复整数性
```

## 论文参数设置（表5-1）

| 参数 | 论文值 | 代码位置 |
|:---|:---|:---|
| f_j (DC固定成本) | [7000, 10000] | DataGenerator.h generateStandard() |
| F_j (订货固定成本) | 100 | config.yaml order_fixed_cost |
| g_j (运输固定成本) | 100 | config.yaml transport_fixed_cost |
| h (库存持有成本) | **10** | config.yaml holding_cost (**当前为1.0, 不一致!**) |
| L_j (补货提前期) | 1 | config.yaml lead_time |
| μ_i (年需求均值) | [2000, 3500] | config.yaml mu_min/mu_max |
| σ²_i (日需求方差) | [10, 15] | config.yaml var_min/var_max |
| v_i (零售商保留价) | [60, 80] | config.yaml reserve_price_min/max |
| f̂_j (设施碳排放) | f_j/10 | fac_carbon_ratio = 0.1 |
| p̂ (碳配额价格) | 2 | config.yaml carbon_price |
| C (初始碳配额) | 100000 | config.yaml carbon_cap |
| ĥ (库存碳排放) | 2 | config.yaml inv_carbon_coeff |
| k (运输碳排放系数) | 0.01 | config.yaml transport_carbon_coeff |
| δ (减排成本系数) | 5000（默认） | config.yaml delta |
| z_α (服务水平) | 1.96 (97.5%) | config.yaml z_alpha |

### 遗传算法参数（论文4.5.1节）

| 参数 | 论文值 | config.yaml |
|:---|:---|:---|
| 种群大小 | 30 | ✅ population_size: 30 |
| 最大迭代 | 50 | ✅ max_generation: 50 |
| 交叉概率 | 0.7 | ✅ crossover_rate: 0.7 |
| 变异概率 | 0.2 | ✅ mutation_rate: 0.2 |
| 编码位数 | 10（3位小数） | ✅ chromosome_length: 10 |

### 运输成本分段函数（论文式5-1,5-2）

供应商→DC: c(a_j):
| 距离 | ≤100 | ≤300 | ≤600 | ≤1000 | >1000 |
|:---|:---|:---|:---|:---|:---|
| 成本 | 5 | 10 | 15 | 20 | 25 |

DC→零售商: c(d_ij):
| 距离 | ≤100 | ≤300 | ≤600 | ≤1000 | >1000 |
|:---|:---|:---|:---|:---|:---|
| 成本 | 10 | 20 | 30 | 40 | 50 |

## 论文表4.1（表5-2）目标结果

| #DCs | #Rts | 投资利润 | 投资碳排放 | 不投资利润 | 不投资碳排放 | 利润增幅 |
|:---|:---|:---|:---|:---|:---|:---|
| 3 | 5 | 458187.2 | 65048.5 | 442819 | 80856 | +3.47% |
| **3** | **10** | **745169.0** | **113038.0** | **703249.4** | **151335.2** | **+5.96%** |
| 5 | 10 | 885151.0 | 119775.2 | 854799.6 | 151916.6 | +3.55% |
| 5 | 20 | 1676735.0 | 199892.0 | 1535646.0 | 298334.2 | +9.18% |
| 5 | 30 | 2250450.0 | 259172.0 | 2175306.0 | 425793.2 | +3.45% |

平均利润增幅 +5.12%，碳排放降幅 -27.63%

**注**: 当前代码尚未验证能否复现这些结果。

## 已知的论文-代码差异

1. **h=10 vs h=1** — 论文表5-1中 h=10，但代码 config.yaml 中 holding_cost: 1.0，Instance 默认 h=1.0
2. **减排投资成本公式** — 论文要求 ½δw²_j√D_j（含√D_j），但代码 BranchAndPrice::solve() 中使用 0.5 * beta_j * w_j²（缺少√D_j因子）。Objective::getR_j_s_Profit 中也没有包含该成本项（设计上放在BP层统一扣除可以，但公式不对）
3. **定价子问题凸包算法** — 代码使用简化的 (xi,yi) = (-μ_i/Ai, -σ²_i/Ai) 凸包枚举，论文 Algorithm 3 更复杂（含余弦相似度、多类边界分类 I1/I2/I3）
4. **初始列生成列** — 代码初始化为空集列+每个零售商单独服务一列；原版代码初始化为仅每个零售商单独服务一列（无空集列）
