# Part 3 / RQ2两阶段实验交接顺序

## 当前阶段

当前只冻结“探索候选池和选择规则”，不冻结最终正式水平。

## Codex后续执行顺序

1. 拉取master并核查正式算法Tag；
2. 确认RQ1已冻结、A3仍暂停；
3. 将旧的90次矩阵标记为`ARCHIVED_DESIGN_DRAFT_NOT_EXECUTED`；
4. 提交新的48次探索矩阵和正式水平选择协议；
5. 审计`investment_exponent`的实际计算路径；
6. 建立独立的Part 3探索队列和全局资源锁；
7. 先运行seed 101的5个边界/基准预检；
8. 独立复算投资成本；
9. 预检通过后运行48次探索；
10. 生成完整探索结果和参数水平诊断；
11. 根据预先声明规则生成5、6、7水平三个候选正式矩阵；
12. 停止，不自动启动90—130次正式实验；
13. 等待用户选择并确认正式矩阵。

## 隔离要求

- 探索批次与正式批次分开；
- 探索结果不进入论文正式统计；
- RQ1、A1、A2、A3原始结果不得修改；
- A3队列不得改变；
- 不修改`src/`和`include/`；
- 不启动Part 4和Part 5。
