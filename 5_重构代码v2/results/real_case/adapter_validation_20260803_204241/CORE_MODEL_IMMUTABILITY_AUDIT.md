# Core model immutability audit

The adapter is a new read-only validator under apps/real_case. No files under src/ or include/ were modified. The frozen model commit remains the worktree base; all changes are classified as new plan/adapter/validation artifacts.
