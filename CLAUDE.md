# CLAUDE.md - 项目指南

## 项目概述
这是一个使用 CPLEX 求解器的优化项目，包含遗传算法（GA）等算法。项目位于 `/home/pei_zeyu/projects/cplex_code/`。

## 记忆文件
项目相关的用户记忆存储于 `/home/pei_zeyu/.claude/memory.md`。每次会话开始时请读取此文件以了解用户背景和偏好。

### 记忆更新规则（重要）
1. **每次调用工具（Bash/Edit/Write等）后**，如果该操作对项目有实质性影响（改代码、跑实验、出结果），立即用一句话追加到 memory.md 的「对话记录」部分
2. **每次收到用户的重要陈述**（偏好变更、发现问题、做出决策），也立即追加
3. **记录格式**：每条一行，以 `YYYY-MM-DD HH:MM` 开头，如：
   `- 2026-07-19 16:00: 修改了 GA 的交叉率参数从 0.8 改为 0.9`
4. **优先保证记录完整性**：如果感觉对话已较长（超过 30 轮交互），主动提出「我来总结一下这段对话到记忆文件」，然后执行写入
5. **写入时机宁可早不要晚**：不要等到对话结束才写

### 上下文长度管理（重要）
用户的 DeepSeek v4 Pro 有 **32 万 token 上下文限制**，API 中转层的自动压缩不可靠。必须主动控制上下文增长：
1. **每完成一个逻辑单元**（改完一个文件、跑完一次实验、调试完一个bug），立即总结到 memory.md，然后提醒用户「是否开启新会话以控制上下文长度」
2. **跑实验时**：实验输出（日志、结果数据）**写入文件**，不要留在对话里。只在对话中展示关键结论（1-3行）
3. **读代码时**：只展示关键片段，不要一次性读出整个大文件。必要时用 `grep` 定位具体行
4. **当对话轮数达到 30 轮时**：主动进行一次记忆总结并建议新会话
5. **大段代码修改**：只展示 diff 而非完整文件内容

## 构建系统
- 使用 `make` 构建
- 二进制文件位于 `../bin/`（ga_bp, run 等）
- 实验配置使用 YAML 格式

## 项目结构
- `5_重构代码v2/` - 主要重构版本
- 包含多种优化算法和实验配置
